<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Joke WebApp</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <main class="container">
    <h1>Joke WebApp</h1>
    <p>Choose options and get jokes from <a href="https://v2.jokeapi.dev/" target="_blank" rel="noopener">JokeAPI</a>.</p>

    <form action="/get-joke" method="POST" class="form">
      <label>
        Your name (optional for personalization)
        <input type="text" name="name" placeholder="Alice" />
      </label>

      <label>
        Category
        <select name="category">
          <% categories.forEach(c => { %>
            <option value="<%= c %>"><%= c %></option>
          <% }) %>
        </select>
      </label>

      <label>
        Joke type
        <select name="type">
          <option value="both">Any</option>
          <option value="single">Single (one-line)</option>
          <option value="twopart">Two-part (setup + delivery)</option>
        </select>
      </label>

      <label class="inline">
        <input type="checkbox" name="safeMode" /> Safe mode (avoid potentially offensive jokes)
      </label>

      <label>
        Amount (1-5)
        <input type="number" name="amount" min="1" max="5" value="1" />
      </label>

      <div class="actions">
        <button type="submit">Get Joke</button>
      </div>
    </form>

    <footer>
      <small>Uses <a href="https://v2.jokeapi.dev/" target="_blank" rel="noopener">JokeAPI v2</a>. No API key required.</small>
    </footer>
  </main>
</body>
</html>
